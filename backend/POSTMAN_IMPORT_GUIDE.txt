═══════════════════════════════════════════════════════════════
   HƯỚNG DẪN IMPORT POSTMAN COLLECTION
═══════════════════════════════════════════════════════════════

📮 BƯỚC 1: Tải và cài đặt Postman
   → https://www.postman.com/downloads/
   → Chọn phiên bản Windows và cài đặt

📮 BƯỚC 2: Mở Postman

📮 BƯỚC 3: Import Collection
   1. Click nút "Import" ở góc trên bên trái
   2. Chọn tab "File"
   3. Click "Choose Files"
   4. Chọn file: D:\QUANLYGIAYADIDA\postman\Adidas_Shop_API.postman_collection.json
   5. Click "Import"

📮 BƯỚC 4: Kiểm tra Collection
   → Bạn sẽ thấy "Adidas Shop API" trong sidebar bên trái
   → Collection có 15 requests được nhóm thành 4 folders:
      • Products (7 requests)
      • Cart (5 requests)
      • Orders (3 requests)
      • Statistics (1 request)

📮 BƯỚC 5: Test API
   1. Mở folder "Products"
   2. Click vào "Get All Products"
   3. Click nút "Send"
   4. Bạn sẽ thấy danh sách 12 sản phẩm giày Adidas

═══════════════════════════════════════════════════════════════
   DANH SÁCH API ENDPOINTS
═══════════════════════════════════════════════════════════════

✅ PRODUCTS (Sản phẩm)
   1. GET    /api/products              - Lấy tất cả sản phẩm
   2. GET    /api/products/:id          - Lấy sản phẩm theo ID
   3. POST   /api/products              - Tạo sản phẩm mới
   4. PUT    /api/products/:id          - Cập nhật sản phẩm
   5. DELETE /api/products/:id          - Xóa sản phẩm
   6. GET    /api/products/category/:category - Lọc theo danh mục
   7. GET    /api/products/search?q=keyword   - Tìm kiếm

✅ CART (Giỏ hàng)
   8. GET    /api/cart                  - Lấy giỏ hàng
   9. POST   /api/cart                  - Thêm vào giỏ hàng
  10. PUT    /api/cart/:productId       - Cập nhật số lượng
  11. DELETE /api/cart/:productId       - Xóa khỏi giỏ hàng
  12. DELETE /api/cart                  - Xóa toàn bộ giỏ hàng

✅ ORDERS (Đơn hàng)
  13. POST   /api/orders                - Tạo đơn hàng
  14. GET    /api/orders                - Lấy danh sách đơn hàng

✅ STATISTICS (Thống kê)
  15. GET    /api/stats                 - Lấy thống kê tổng quan

═══════════════════════════════════════════════════════════════
   TEST SCENARIOS
═══════════════════════════════════════════════════════════════

🧪 Scenario 1: Xem và tìm kiếm sản phẩm
   1. Get All Products
   2. Search Products (q=ultraboost)
   3. Get Products By Category (running)

🧪 Scenario 2: Mua hàng
   1. Get All Products
   2. Add To Cart (productId=1, quantity=2)
   3. Get Cart
   4. Create Order
   5. Clear Cart

🧪 Scenario 3: Quản lý sản phẩm (Admin)
   1. Create Product
   2. Get Product By ID
   3. Update Product
   4. Delete Product

🧪 Scenario 4: Thống kê
   1. Get Stats

═══════════════════════════════════════════════════════════════
   TIPS & TRICKS
═══════════════════════════════════════════════════════════════

💡 Sử dụng Variables
   → Collection đã có biến {{base_url}} = http://localhost:3001/api
   → Bạn có thể thay đổi trong Collection settings

💡 Xem Response
   → Tab "Body" hiển thị JSON response
   → Tab "Headers" hiển thị HTTP headers
   → Tab "Test Results" hiển thị kết quả test

💡 Save Response
   → Click "Save Response" để lưu response mẫu
   → Dùng để so sánh với response sau này

💡 Collection Runner
   → Click vào collection → "Run"
   → Chạy tất cả requests một lúc để test toàn bộ API

═══════════════════════════════════════════════════════════════
   TROUBLESHOOTING
═══════════════════════════════════════════════════════════════

❌ Lỗi: Could not get response
   ✅ Kiểm tra Docker containers đang chạy: docker ps
   ✅ Kiểm tra API: http://localhost:3001/api/products

❌ Lỗi: 404 Not Found
   ✅ Kiểm tra URL endpoint
   ✅ Kiểm tra method (GET, POST, PUT, DELETE)

❌ Lỗi: 500 Internal Server Error
   ✅ Xem logs: docker-compose logs api
   ✅ Kiểm tra request body format

═══════════════════════════════════════════════════════════════
